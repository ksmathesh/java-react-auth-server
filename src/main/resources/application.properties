spring.application.name=secauth

# Database Configuration (MySQL)
# Make sure to create a database named 'secauth' in your MySQL instance.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/secauth?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Mathesh@2007}

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT Configuration
# Note: In production, use a strong, secure secret key stored in environment variables.
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration-ms=86400000
